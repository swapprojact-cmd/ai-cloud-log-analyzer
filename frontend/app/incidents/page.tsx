"use client";
import {useEffect,useState} from "react"; import {api,Incident} from "../../lib/api";
export default function Incidents(){const[data,setData]=useState<Incident[]>([]);const[error,setError]=useState("");async function load(){const r=await api<{incidents:Incident[]}>("/api/incidents");setData(r.incidents||[])}useEffect(()=>{load().catch(e=>setError(e.message))},[]);async function resolve(id:string){try{await api(`/api/incidents/${id}`,{method:"PATCH",body:JSON.stringify({status:"resolved"})});await load()}catch(e){setError(e instanceof Error?e.message:"Update failed")}}return <main style={{maxWidth:1000,margin:"40px auto",padding:24,fontFamily:"sans-serif"}}><h1>Incidents</h1>{error&&<p>{error}</p>}{data.map(i=><article key={i.id} style={{padding:16,border:"1px solid #ddd",marginBottom:10}}><h2>{i.title}</h2><p>{i.severity} · {i.status}</p><p>{i.description}</p><pre>{i.ai_explanation}</pre>{i.status!=="resolved"&&<button onClick={()=>resolve(i.id)}>Mark resolved</button>}</article>)}</main>}
