"use client";
import {useEffect,useState} from "react";
import {api} from "../../lib/api";
import {LineChart,Line,XAxis,YAxis,Tooltip,CartesianGrid,ResponsiveContainer} from "recharts";
type Trend={date:string;logs:number;errors:number;warnings:number};
export default function Analytics(){const[data,setData]=useState<Trend[]>([]);const[error,setError]=useState("");useEffect(()=>{api<{trends:Trend[]}>("/api/dashboard/trends").then(r=>setData(r.trends||[])).catch(e=>setError(e instanceof Error?e.message:"Could not load analytics"))},[]);const total=data.reduce((n,d)=>n+(d.logs||0),0),errors=data.reduce((n,d)=>n+(d.errors||0),0),warnings=data.reduce((n,d)=>n+(d.warnings||0),0);return <div className="app-shell"><nav className="nav container"><a className="brand" href="/dashboard"><span className="logo">AI</span><span>Cloud Log Analyzer</span></a><div className="navlinks"><a href="/dashboard">Overview</a><a href="/logs">Logs</a><a href="/incidents">Incidents</a><a className="active" href="/analytics">Analytics</a></div><a className="btn" href="/dashboard">Dashboard</a></nav><main className="container app-main"><div className="page-head"><div><div className="eyebrow"><span className="dot"/> Observability analytics</div><h1>Understand your system.</h1><p>Track log volume and error patterns over time.</p></div><a className="btn btn-primary" href="/logs">Open Log Explorer</a></div><section className="metric-grid"><div className="card metric"><div className="label2">Log volume</div><div className="value">{total}</div><div className="trend">Across available trend data</div></div><div className="card metric"><div className="label2">Errors</div><div className="value">{errors}</div><div className="trend">Error-level events</div></div><div className="card metric"><div className="label2">Warnings</div><div className="value">{warnings}</div><div className="trend">Warning-level events</div></div><div className="card metric"><div className="label2">Data points</div><div className="value">{data.length}</div><div className="trend">Reported time periods</div></div></section><section className="card chart-card"><div className="section-title"><div><h2>Log activity</h2><p className="muted">Daily logs, errors and warnings.</p></div><span className="badge">Live data</span></div>{error?<div className="error">{error}</div>:data.length===0?<div className="empty"><div className="empty-icon">⌁</div><strong>No analytics data yet</strong><span>Analyze some logs to populate your observability trends.</span></div>:<div className="chart-wrap"><ResponsiveContainer width="100%" height={420}><LineChart data={data} margin={{top:10,right:15,left:0,bottom:5}}><CartesianGrid strokeDasharray="3 3" stroke="rgba(255,255,255,.08)"/><XAxis dataKey="date" stroke="#91a0b5"/><YAxis stroke="#91a0b5"/><Tooltip contentStyle={{background:"#0d1a2b",border:"1px solid #203149",borderRadius:10,color:"#fff"}}/><Line type="monotone" dataKey="logs" stroke="#67e8f9" strokeWidth={3} dot={false}/><Line type="monotone" dataKey="errors" stroke="#fb7185" strokeWidth={2} dot={false}/><Line type="monotone" dataKey="warnings" stroke="#fbbf24" strokeWidth={2} dot={false}/></LineChart></ResponsiveContainer></div>}</section></main></div>}
